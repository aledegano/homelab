apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: grafana
spec:
  rules:
  - host: grafana.raspi.hal
    http:
      paths:
      - path: /
        backend:
          serviceName: grafana
          servicePort: grafana-web