---
all:
  hosts:
    raspi:
  vars:
    ssh_signer_hostname: 'raspi,192.168.1.116'
    ssh_username: hal
    ansible_ssh_common_args: '-o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null'
    ansible_ssh_user: pi
    ansible_ssh_pass: raspberry
    ssh_public_key: ~/.ssh/id_rsa_raspberry.pub
